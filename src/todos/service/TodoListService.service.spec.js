describe('TodoListService', function() {

    beforeEach(module('todos'));

    it('should ...', inject(function(TodoListService) {

        //expect(TodoListService.doSomething()).toEqual('something');

    }));

});
